# Triage: issue #25 — Source health, week of 2026-06-29

- **Issue:** https://github.com/frankdevlabs/cada-2026-0138/issues/25
- **Source:** weekly source-health rollup (`T1-03`, `T2-01`)
- **Detected:** 2026-06-29
- **Type / playbook:** source-health / source-health

## What was flagged

The weekly health rollup (2026-06-29T11:50:44Z) reports two non-recovered sources; the other 15
watched sources are reachable (T1-12 recovered earlier, HTTP 200):

| Code | Source | Status this run | Consec. | Since |
|---|---|---|---|---|
| T1-03 | EUR-Lex HIS page | HTTP 202 AWS-WAF challenge — **auto-paused** | #14 | paused since 2026-06-24 |
| T2-01 | Tech Policy Press `/feed/` | Next.js SPA shell, 0 `<item>`s | #4 | 2026-06-24 |

## Assessment

One by-design dead end, one genuinely moved feed.

- **T1-03 — unreachable by design (WAF); already auto-paused, no action.** Live re-check during this
  triage (2026-06-29): `https://eur-lex.europa.eu/legal-content/EN/HIS/?uri=CELEX:52026PC0502` →
  **HTTP 202** AWS-WAF challenge, confirming the rollup. The source hit the 14-day threshold and is
  auto-paused since 2026-06-24, exactly as the watchlist `notes` anticipate ("EUR-Lex bot-blocks
  (202/WAF); prefer the Cellar endpoint T1-02 when this fails"). The procedure-history coverage it
  provides is fully redundant with **T1-01** (OEIL procedure file) and **T1-02** (Cellar CELEX
  52026PC0502); there is no content loss. Resume requires manual (browser/WAF-solving) intervention,
  which is not warranted while OEIL covers the same history. **Keep the URL as-is; keep it paused.**

- **T2-01 — feed has MOVED; re-point.** This is the 4th consecutive failure (since 2026-06-24) and the
  earlier rollup (#10, 2026-06-12/16) already flagged the same app-shell behaviour, so it is no longer
  a transient flap. Root cause confirmed live (2026-06-29): `https://www.techpolicy.press/feed/` (and
  `/rss/`, `/feed.xml`, `/index.xml`, etc.) all return the site's Next.js SPA HTML shell — the WordPress
  `/feed/` endpoint is gone after a platform migration. The site's own RSS autodiscovery `<link>` on the
  homepage points to **`https://techpolicy.press/rss/feed.xml`** (→ 301 to the `www` host). That URL
  returns valid `<rss version="2.0">` XML with live `<item>`s — and is on-topic: the top item on
  2026-06-29 is *"How AI Keeps Europe Hooked on US cloud"*. The feed did not die; it relocated.

Scope: a fetch-health maintenance rollup, not a content change on `2026/0138 (COD)`; nothing concerns
the `keep_apart` Chips Act 2.0 sibling (2026/0139). In scope as maintenance only.

Note (not actioned): the resolver's `affects` lists `data/watchlist.yaml`, which does not exist — the
watchlist lives in [`tracker.yaml`](../../tracker.yaml). Known helper-metadata quirk (see #10 triage).
Neither [`data/documents.yaml`](../../data/documents.yaml) nor [`sources/README.md`](../../sources/README.md)
references Tech Policy Press as a registered document, so neither changes.

## Plan

1. **T2-01 — re-point the feed URL** in [`tracker.yaml`](../../tracker.yaml) (`watchlist.sources.T2-01.url`)
   from `https://www.techpolicy.press/feed/` to `https://www.techpolicy.press/rss/feed.xml`, and add a
   `notes` line recording the WordPress→Next.js migration so a future shell-response is not re-diagnosed
   from scratch.
2. **T1-03 — no change.** Leave the URL and `notes` as-is; the auto-pause mechanism is working as
   designed and coverage is redundant via T1-01/T1-02.
3. No edits to [`data/documents.yaml`](../../data/documents.yaml) or [`sources/README.md`](../../sources/README.md)
   (TPP is not a registered committed document; the article cited in
   [`docs/stakeholders.md`](../../docs/stakeholders.md) is a fixed permalink, unaffected).
4. Do **not** close issue #25 — it is the single weekly health rollup, updated/commented in place on
   subsequent runs. Keep it open until both sources recover/are re-pointed; the T2-01 re-point should
   clear it on the next run, leaving only the by-design T1-03 pause.

## Verification

- Live re-checks (2026-06-29): old `…/feed/` → Next.js HTML shell, 0 `<item>`s; new
  `https://www.techpolicy.press/rss/feed.xml` → `<rss 2.0>` with live dated `<item>`s; T1-03 HIS →
  HTTP 202 WAF (confirms the auto-pause).
- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → must end "0 broken".
- Next weekly tracker run should baseline T2-01 against the new URL (HTTP 200 + items) and update the
  rollup, leaving T1-03 as the only remaining (expected) paused source.
