# Triage: issue #14 — Source health, week of 2026-06-22

- **Issue:** https://github.com/frankdevlabs/cada-2026-0138/issues/14
- **Source:** weekly source-health rollup (`T1-01`, `T1-02`, `T1-03`, `T1-05`, `T1-08`, `T1-09`, `T1-10`, `T1-12`, `T2-02`, `T2-04`)
- **Detected:** 2026-06-22
- **Type / playbook:** source-health / source-health

## What was flagged

The weekly health rollup (as of 2026-06-22T11:48:51Z) reports fetch errors on four sources and
non-error hash drift on five more. Only the four are genuine fetch failures; the rest drifted on
rotating/CDN/feed content with no substantive change.

| Code | Source | Status this run | Consec. | Since |
|---|---|---|---|---|
| T1-03 | EUR-Lex HIS page (CELEX 52026PC0502) | HTTP 202 AWS-WAF challenge | 9 | 2026-06-10 |
| T1-09 | officielebekendmakingen.nl dossier 22112 (BNC-fiches) | HTTP 500 | 1 | 2026-06-22 |
| T1-10 | officielebekendmakingen.nl dossier 21501-33 (Telecomraad) | HTTP 500 | 1 | 2026-06-22 |
| T1-12 | officielebekendmakingen.nl RSS (Kamervragen cloud/AI) | HTTP 500 | 1 | 2026-06-22 |
| T1-01 | OEIL procedure file 2026/0138(COD) | hash drift, no substantive change | — | 2026-06-22 |
| T1-05 | Commission digital-strategy CADA policy page | hash drift (rotating) | — | 2026-06-22 |
| T1-08 | EESC CADA opinion page | hash drift (CDN), opinion unchanged | — | 2026-06-22 |
| T2-02 | dutchdatacenters.nl/nieuws | hash drift (nav/newsletter boilerplate) | — | 2026-06-22 |
| T2-04 | INSIGHT EU MONITORING RSS | feed rotation, 0 CADA matches | — | 2026-06-22 |

(`T1-02` appears in the helper's parsed `codes` only because it is named in the T1-03 note as the
fallback endpoint — it was not itself flagged this run.)

## Assessment

**All transient or by-design; no source is stale, moved, or defunct. No URL needs changing.**
Per-source verdict:

- **T1-09 / T1-10 / T1-12 — same-host outage on `zoek.officielebekendmakingen.nl`.** All three are
  on that one host and were healthy/baselined on 2026-06-19; the simultaneous HTTP 500 across all
  three is a single host-wide outage, not three independent faults — a recurrence of the 2026-06-16
  outage seen in issue #10. Live re-check during this triage (2026-06-22) shows the host **still
  returning HTTP 500** (dossiers 22112 and 21501-33). Unlike #10 (recovered by re-check time), it had
  not recovered yet, but this is only 1 consecutive failure on a host with a known recurring 500 flap,
  the URLs are correct, and the tracker did not update the hashes and will retry next run. Not yet
  actionable; watch for it persisting across multiple runs.
- **T1-03 — unreachable by design (WAF), expected.** EUR-Lex HIS bot-blocks (202/WAF). The watchlist
  `notes` already say to prefer the Cellar endpoint (T1-02) when it fails; live re-check (2026-06-22)
  confirms the Cellar URI content-negotiates normally (`303 → cellar object`), so the proposal's
  machine-readable text remains reachable. The tracker auto-pauses T1-03 at 14 consecutive days
  (~2026-06-24, now 2 days out). No action — the fallback covers it.
- **T1-01 — rotating page element survives normalization.** OEIL hash drifted again with no
  substantive change (identical visible content to issue #12: rapporteur still "Appointed — pending
  final decision on the referral", key events still only 03/06 COM(2026)0502). Tracker updated the
  hash; not flagged. No action.
- **T1-05 / T1-08 / T2-02 / T2-04 — rotating/CDN/feed noise.** T1-05 "Last update 3 June 2026"
  unchanged; T1-08 EESC opinion INT/1126 (rapporteur Hajnoš, plenary 608 / 23-24 Sep 2026) unchanged;
  T2-02 only nav/newsletter boilerplate; T2-04 feed rotation with 0 CADA matches. Hashes updated,
  none flagged. No action.

Scope: this is a fetch-health rollup, not a content change on `2026/0138 (COD)`; nothing in it
concerns the `keep_apart` Chips Act 2.0 sibling (2026/0139). In scope as a maintenance item only.

Note (not actioned here, carried from issue #10): the resolver's `affects` lists `data/watchlist.yaml`,
which does not exist — the watchlist lives in [`tracker.yaml`](../../tracker.yaml). Helper-metadata
quirk, out of scope.

## Plan

**No repo edits.** Every flagged source is a transient (HTTP 500 host outage) or by-design
(WAF 202, with a confirmed-healthy fallback) failure, or non-error rotating-content hash drift —
each with a correct, still-current URL. There is no stale URL to correct, no fallback to add, and no
source to mark unreachable or remove from [`tracker.yaml`](../../tracker.yaml) /
[`data/documents.yaml`](../../data/documents.yaml).

1. Leave [`tracker.yaml`](../../tracker.yaml) watchlist URLs unchanged.
2. Leave [`data/documents.yaml`](../../data/documents.yaml) and [`sources/README.md`](../../sources/README.md) unchanged.
3. Re-check next weekly run, with attention to: **T1-09/T1-10/T1-12** — escalate (corrected URL or
   alternate endpoint) only if `zoek.officielebekendmakingen.nl` stays 500 across multiple runs rather
   than the usual one-run flap; and **T1-03** crossing its ~2026-06-24 auto-pause threshold (expected,
   fallback T1-02 covers it).

## Verification

- Live re-check (2026-06-22): `zoek.officielebekendmakingen.nl` dossiers → **HTTP 500** (outage
  ongoing); Cellar CELEX 52026PC0502 → **HTTP 303** (fallback healthy); EUR-Lex HIS → **HTTP 202**
  (WAF, by design).
- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → "0 broken".
- This is the week-of-2026-06-22 rollup issue; closes on merge of this plan PR.
