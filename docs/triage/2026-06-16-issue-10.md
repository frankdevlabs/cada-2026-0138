# Triage: issue #10 — Source health, week of 2026-06-16

- **Issue:** https://github.com/frankdevlabs/cada-2026-0138/issues/10
- **Source:** weekly source-health rollup (`T1-02`, `T1-03`, `T1-09`, `T1-10`, `T1-12`, `T2-01`, `T2-02`)
- **Detected:** 2026-06-16
- **Type / playbook:** source-health / source-health

## What was flagged

The weekly health rollup reports fetch failures / anomalies on seven watched sources (as of
2026-06-16T11:49:20Z). None had hit the 14-day auto-pause threshold:

| Code | Source | Status this run | Consec. | Since |
|---|---|---|---|---|
| T1-02 | Cellar (CELEX 52026PC0502) | HTTP 300 content-negotiation list (not the XML notice) | 1 | 2026-06-16 |
| T1-03 | EUR-Lex HIS page | HTTP 202 AWS-WAF challenge | 5 | 2026-06-10 |
| T1-09 | officielebekendmakingen.nl dossier 22112 (BNC-fiches) | HTTP 500 | 1 | 2026-06-16 |
| T1-10 | officielebekendmakingen.nl dossier 21501-33 (Telecomraad) | HTTP 500 | 1 | 2026-06-16 |
| T1-12 | officielebekendmakingen.nl RSS (Kamervragen cloud/AI) | HTTP 500 | 1 | 2026-06-16 |
| T2-01 | techpolicy.press `/feed/` | Next.js app shell, 0 RSS items (recurring) | — | 2026-06-12 |
| T2-02 | dutchdatacenters.nl/nieuws | CDN cache flap (hash reverted to older page) | — | 2026-06-16 |

## Assessment

**All transient; no source is stale, moved, or defunct. No URL needs changing.** Per-source verdict:

- **T1-09 / T1-10 / T1-12 — same-host outage, already recovered.** All three are on
  `zoek.officielebekendmakingen.nl` and were healthy/baselined on 2026-06-15; the simultaneous
  HTTP 500 across all three is a single host-wide outage today, not three independent faults.
  Live re-check during this triage (2026-06-16): the host returns **HTTP 200** and dossier 22112
  renders normally. Recovered.
- **T1-02 — content-negotiation flap, already recovered.** The HTTP 300 was a manifestation list
  rather than the machine-readable XML notice; the tracker correctly did **not** update the hash.
  Live re-check: the Cellar URI now content-negotiates normally (303 → cellar object). Recovered.
  This source is only the machine-readable fallback for the rarely-changing base text (T1-01/T1-06
  cover the same proposal).
- **T1-03 — unreachable by design (WAF), expected.** EUR-Lex HIS bot-blocks (202/WAF); the watchlist
  `notes` already say to prefer the Cellar endpoint (T1-02) when it fails. The tracker's own logic
  will auto-pause this at 14 consecutive days (~2026-06-24). No action until then.
- **T2-01 — recurring app-shell, no data loss.** The Next.js shell yields 0 RSS items; the tracker
  did not update the hash and retries. Annoying but not a content miss. Watch; if it persists for
  weeks, consider an alternate Tech Policy Press endpoint — not yet.
- **T2-02 — CDN cache flap.** Hash reverted to a pre-2026-06-15 cached page; the 2026-06-15 baseline
  was retained and there was no CADA keyword match either way. No action.

Scope: this is a fetch-health rollup, not a content change on `2026/0138 (COD)`; nothing in it
concerns the `keep_apart` Chips Act 2.0 sibling. In scope as a maintenance item only.

Note (not actioned here): the resolver's `affects` lists `data/watchlist.yaml`, which does not exist
in this repo — the watchlist lives in [`tracker.yaml`](../../tracker.yaml). That is a helper-metadata
quirk, out of scope for this issue.

## Plan

**No repo edits.** Every flagged source is a transient/by-design failure with a correct,
still-current URL; two were re-verified live as already recovered during this triage. There is no
stale URL to correct, no fallback to add, and no source to mark unreachable or remove from
[`tracker.yaml`](../../tracker.yaml) / [`data/documents.yaml`](../../data/documents.yaml).

1. Leave [`tracker.yaml`](../../tracker.yaml) watchlist URLs unchanged.
2. Leave [`data/documents.yaml`](../../data/documents.yaml) and [`sources/README.md`](../../sources/README.md) unchanged.
3. Do **not** close issue #10 — it is the single weekly health rollup, updated/commented in place
   on subsequent runs rather than reopened. Keep it open.
4. Re-check next weekly run, with attention to: T1-03 approaching its ~2026-06-24 auto-pause, and
   T2-01 if the app-shell/0-items behaviour keeps recurring.

## Verification

- Live re-check (2026-06-16) confirmed `zoek.officielebekendmakingen.nl` → HTTP 200 and the Cellar
  CELEX URI → 303 content-negotiation, i.e. both recovered.
- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → "0 broken".
- Leave #10 open; next weekly tracker run updates the rollup table in place.
